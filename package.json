{
  "name": "spartner-investor-portal",
  "version": "0.1.0",
  "private": true,
  "description": "SPARTNER Investor Portal MVP",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "echo build",
    "start": "node --env-file=.env server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "nodemailer": "^9.0.5",
    "xlsx": "^0.18.5"
  }
}